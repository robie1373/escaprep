import React, { Component } from 'react';


class DescribeApp extends Component {
  render () {
    return (
      <div className="appDescription">
        <p>This page is used to import a single csv file from Phishme, rearrange the columns to support the powershell script and allow the user to download the new file.</p>
      </div>
    );
  }
}

export default DescribeApp;
